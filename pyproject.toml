[project]
name = "material-harbor"
version = "0.1.0"
description = "Harbor adapters for scientific benchmark datasets"
requires-python = ">=3.11"
dependencies = ["datasets>=3.0", "pandas>=2.0", "pillow>=10.0", "pyarrow>=14.0", "requests>=2.31"]
