# SciAgentGYM scientific tool-use task

Solve the following {{ subject }} / {{ topic }} problem. You may inspect the benchmark case at `/app/data/case.json` and use Python or other scientific tools available in the environment. Work through the required multi-step computation rather than guessing.

## Problem
{{ question }}

Expected tool concepts (when applicable): {{ tools }}

Write the final answer to `/app/answer.txt`. Match the benchmark answer as closely as possible; for structured answers, write valid JSON.
